# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.5-browsers

    working_directory: ~/BD_IVT_17_3

    steps:
      - checkout

      - run:
          name: run 
          command: |
            . venv/bin/activate
            python run_validation.py
